services:
  ai-proxy:
    build: .
    image: holdltd/ai-proxy
    container_name: ai-proxy
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "5234:8080"
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data        # tokens.json + audit_logs/ live here
